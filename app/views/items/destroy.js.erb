<% if @item.destroyed? %>
  $('#errors-explanation').prepend("<div class='alert alert-dismissible alert-success'><button type='button' class='close' data-dismiss='alert'>×</button><%= flash.discard(:notice) %></div>");
  $('#task-'+<%= @item.id%>).hide();
<% else %>
  $('#task-'+<%= @item.id%>).prepend("<div class='alert alert-danger'><%= flash.discard(:error) %></div>");
<% end %>